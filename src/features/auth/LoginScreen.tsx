// Stubbed in Task 9 so Routes compiles. Real implementation in Task 11.
export default function LoginScreen() {
  return (
    <div className="p-6 text-ink">
      <h1 className="text-2xl font-bold">Login (stub)</h1>
      <p className="text-muted">Phase 1 Task 11 fills this in.</p>
    </div>
  );
}
